#!/usr/bin/env python
# -*- coding: CP1252 -*-
#
# generated by wxGlade 0.6.7 (standalone edition) on Thu Oct 24 22:32:51 2013
#

import wx
import funciones as f
# begin wxGlade: dependencies
import gettext
# end wxGlade

# begin wxGlade: extracode
# end wxGlade


class MyDialog(wx.Dialog):
    def __init__(self, *args, **kwds):
        # begin wxGlade: MyDialog.__init__
        kwds["style"] = wx.DEFAULT_DIALOG_STYLE
        wx.Dialog.__init__(self, *args, **kwds)
        self.control_lista = wx.ListCtrl(self, wx.ID_ANY, style=wx.LC_REPORT | wx.SUNKEN_BORDER)
        self.button_1 = wx.Button(self, wx.ID_ANY, _("Aceptar"))

        self.__set_properties()
        self.__do_layout()

        self.Bind(wx.EVT_BUTTON, self.OnAceptar, self.button_1)
        # end wxGlade

    def __set_properties(self):
        # begin wxGlade: MyDialog.__set_properties
        self.SetTitle(_("dialog_1"))
        # end wxGlade

    def __do_layout(self):
        # begin wxGlade: MyDialog.__do_layout
        sizer_1 = wx.BoxSizer(wx.VERTICAL)
        sizer_1.Add(self.control_lista, 1, wx.EXPAND, 0)
        sizer_1.Add(self.button_1, 0, wx.ALIGN_CENTER_HORIZONTAL, 0)
        self.SetSizer(sizer_1)
        sizer_1.Fit(self)
        self.Layout()
        # end wxGlade
        self.crearListado()
        
    def crearListado(self):
        self.control_lista.InsertColumn(0, "Usuario")
        self.control_lista.InsertColumn(1, "Fecha")
        #self.control_lista.InsertColumn(1, "Hora")
        self.currentItem = 0
        
    def poblarListado(self,tipo):
        self.control_lista.DeleteAllItems() #limpia la lista
        # invoca la función que crea una lista de registros
        # seguń tipo (Móvil o Fijo)
        data = f.Reporte(tipo) 
        # llena el control de lista
        for x in range(len(data)):
            tel = data[x]
            self.control_lista.InsertStringItem(x, str(tel[0]))
            self.control_lista.SetStringItem(x, 1, str(tel[1]))
            #self.control_lista.SetStringItem(x, 1, str(tel[2]))
        # genera las dimensiones de las columnas
        self.control_lista.SetColumnWidth(0, 80)
        self.control_lista.SetColumnWidth(1, wx.LIST_AUTOSIZE)
        #self.control_lista.SetColumnWidth(2, wx.LIST_AUTOSIZE)

    def OnAceptar(self, tipo):  # wxGlade: MyDialog.<event_handler>
        self.control_lista.DeleteAllItems() #limpia la lista
        # invoca la función que crea una lista de registros
        # seguń tipo (Móvil o Fijo)
        data = f.Reporte(tipo) 
        # llena el control de lista
        for x in range(len(data)):
            tel = data[x]
            self.control_lista.InsertStringItem(x, str(tel[0]))
            self.control_lista.SetStringItem(x, 1, str(tel[1]))
            #self.control_lista.SetStringItem(x, 1, str(tel[2]))
        # genera las dimensiones de las columnas
        self.control_lista.SetColumnWidth(0, 80)
        self.control_lista.SetColumnWidth(1, wx.LIST_AUTOSIZE)
        #self.control_lista.SetColumnWidth(2, wx.LIST_AUTOSIZE)

# end of class MyDialog
if __name__ == "__main__":
    gettext.install("app") # replace with the appropriate catalog name

    app = wx.App()
    #wx.InitAllImageHandlers()
    dialog_1 = MyDialog(None, wx.ID_ANY, "")
    app.SetTopWindow(dialog_1)
    dialog_1.Show()
    app.MainLoop()
